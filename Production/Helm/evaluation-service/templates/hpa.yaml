{{- if (index .Values "evaluation-service").autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: evaluation-service-hpa
  namespace: {{ (index .Values "evaluation-service").namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: app-evaluation-service
  minReplicas: {{ (index .Values "evaluation-service").autoscaling.minReplicas }}
  maxReplicas: {{ (index .Values "evaluation-service").autoscaling.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ (index .Values "evaluation-service").autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ (index .Values "evaluation-service").autoscaling.targetMemoryUtilizationPercentage }}
{{- end }}
